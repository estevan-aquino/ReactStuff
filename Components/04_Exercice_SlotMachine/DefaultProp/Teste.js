class Teste extends React.Component {
    render () {
        return (
            <div>
                <p>Hi! To {this.props.to} From {this.props.from}{info}</p> 
            </div>
        )
    }
}